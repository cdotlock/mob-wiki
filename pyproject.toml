[project]
name = "mob-wiki"
version = "0.1.0"
description = "Team LLM knowledge base — Git-backed wiki with MCP server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=4.0,<5",
    "Markdown>=3.5",
    "pyyaml>=6.0",
    "uvicorn>=0.29",
    "bleach>=6.0",
    "filelock>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[project.scripts]
mob-wiki = "server:main"

[tool.setuptools]
packages = []
py-modules = ["server", "indexer", "wiki_links"]
